- title @notice.name
- actions do
  = link_to edit_notebook_section_notice_path(id: @notice.id) do
    %button.mdl-button.mdl-js-button.mdl-button--icon
      %i.material-icons edit
  = link_to notebook_section_notice_path(id: @notice.id), method: :delete, data: { confirm: t('are_you_sure') } do
    %button.mdl-button.mdl-js-button.mdl-button--icon
      %i.material-icons delete

.mdl-grid
  = box do
    = @notice.text
