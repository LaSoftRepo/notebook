<% provide :page_title, 'Notebooks' %>
<div class="list-group">
  <% notebooks.each do |notebook| %>
    <%= link_to notebook.name, notebook_path(notebook), class: 'list-group-item list-group-item-action' %>
  <% end %>
</div>
