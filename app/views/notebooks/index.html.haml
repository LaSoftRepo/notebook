- title t('title.notebook.index')

- if @notebooks.any?
  - @notebooks.each do |notebook|
    .box.mdl-cell.mdl-cell--3-col.mdl-cell--4-col-tablet.mdl-cell--4-col-phone
      = link_to notebook.name, notebook_sections_path(notebook_id: notebook.id)
- else
  = box do
    You have no notebooks. Add the first one.

= add_fixed_button link: new_notebook_path
